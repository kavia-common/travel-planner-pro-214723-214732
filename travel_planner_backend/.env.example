# Backend environment variables (copy to .env in deployment environment; do not commit secrets)
#
# These are provided by the database container in this project:
POSTGRES_URL=localhost
POSTGRES_USER=appuser
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=myapp
POSTGRES_PORT=5000
